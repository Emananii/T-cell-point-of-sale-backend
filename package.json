{
  "name": "t-cell-point-of-sale-backend",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "json-server --host 0.0.0.0 --watch db.json --port $PORT"
  },
  "dependencies": {
    "json-server": "^0.17.0"
  }
}
